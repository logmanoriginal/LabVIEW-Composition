This directory contains tests that require mulitple build steps to execute. Tests are organized by topic and include their own dedicated pipeline tasks.

[`all-tests.yml`](all-tests.yml) imports the individual test steps and is included in the main pipeline